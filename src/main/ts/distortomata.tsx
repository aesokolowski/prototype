import React from 'react';
import ReactDOM from 'react-dom';

import Distortomata from './distortomata/Distortomata';

ReactDOM.render(
    <Distortomata />,
    document.getElementById('distortomata')
);